import 'package:flutter/material.dart';

/// Palette for Chrono-Swipe. Dark field with neon accent colors: cyan (the
/// Chrono-Shift accent), gold (the at-speed accent), red (danger), plus a
/// near-black background.
class Palette {
  Color get background => const Color(0xff05060c);
  Color get gold => const Color(0xffffc857);
  Color get cyan => const Color(0xff2ff3e0);
  Color get red => const Color(0xffff3b5c);
  Color get white => const Color(0xffffffff);
  Color get dim => const Color(0xff8892a6);
}
